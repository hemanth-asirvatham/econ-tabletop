Create a 3:2 horizontal DEVELOPMENT card illustration with baked-in text.

Scenario context: {{ scenario_injection or "(none)" }}
Locale visuals (optional motifs): {{ locale_visuals | join(", ") }}
Card format notes (concise): {{ image_outline_text }}

Title (verbatim): "{{ card.title }}"
Description (verbatim): "{{ card.short_description }}"
Stage label: "STAGE {{ card.stage }}"
Arrows up/down: {{ card.arrows_up }} up, {{ card.arrows_down }} down
Severity: {{ card.severity }}/5

Layout & styling:
- Title in a clean top band; description in the body with high legibility.
- Small "DEVELOPMENT" label in an upper corner; "STAGE {{ card.stage }}" label upper-left.
- Small "OUTCOME" label upper-right above caret icons.
- Show stacked caret triangles for outcomes: {{ card.arrows_up }} green up-carets and {{ card.arrows_down }} red down-carets; if both are zero, use a single neutral gray line.
- Include a labeled severity indicator (SEVERITY) with filled dots or bars matching the score above.
- Minimalist background motif reinforcing the development theme; thin line icons; generous whitespace.
- Muted palette (2–3 colors), soft gradients, subtle texture; avoid photorealism.
- Only show the specified text/labels; no extra readable text.
