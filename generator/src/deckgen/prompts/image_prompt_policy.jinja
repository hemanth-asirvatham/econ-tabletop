Create a 3:2 horizontal POLICY card illustration with baked-in text.

Scenario context: {{ scenario_injection or "(none)" }}
Locale visuals (optional motifs): {{ locale_visuals | join(", ") }}
Card format notes (concise): {{ image_outline_text }}

Title (verbatim): "{{ card.title }}"
Description (verbatim): "{{ card.short_description }}"
Impact score: {{ card.impact_score }}/5
Budget level: {{ card.cost.budget_level }}/5
Complexity: {{ card.cost.implementation_complexity }}/5

Layout & styling:
- Title in a clean top band; description in the body with high legibility.
- Small "POLICY" label in an upper corner.
- Include labeled score chips/bars (IMPACT, BUDGET, COMPLEXITY) with filled dots or bars matching the numbers above.
- Minimalist background motif reinforcing the policy theme; thin line icons; generous whitespace.
- Muted palette (2–3 colors), soft gradients, subtle texture; avoid photorealism.
- Only show the specified text/labels; no extra readable text.
